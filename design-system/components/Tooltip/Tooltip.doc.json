{
  "name": "Tooltip",
  "category": "Overlays and layering",
  "status": "stable",
  "aaReviewed": "2026-07-16",
  "library": "Material Components Web (MDC) 14.0.0 — Cartrack-themed",
  "source": "md2-cartrack-library/components/tooltips.html",
  "description": "Tooltips reveal a short text label on hover or focus — naming an icon-only button, or showing a value that's been truncated in a dense table. They're especially valuable on desktop, where hover exists.",
  "keyRule": "Use a tooltip for brief, supplementary text — an icon's name, a truncated cell's full value. Never put essential information or actions only in a tooltip.",
  "variants": [
    {
      "name": "Plain",
      "whenToUse": "A short label naming or clarifying the trigger."
    },
    {
      "name": "Truncation reveal",
      "whenToUse": "Shows the full value when text is clipped in a cell/label."
    }
  ],
  "anatomy": [
    {
      "name": "Trigger",
      "description": "the element being described (icon button, truncated text)."
    },
    {
      "name": "Container",
      "description": "the small dark surface."
    },
    {
      "name": "Label",
      "description": "concise text (ideally a few words)."
    }
  ],
  "behaviors": [
    {
      "name": "Trigger",
      "description": "Appears on hover and on keyboard focus; dismisses on blur/mouse-out or Escape."
    },
    {
      "name": "Delay",
      "description": "A short enter delay avoids tooltip noise as the pointer moves across a toolbar."
    },
    {
      "name": "Positioning",
      "description": "Flips to stay within the viewport; never covers its own trigger."
    }
  ],
  "states": [
    {
      "state": "Hidden",
      "treatment": "Not rendered until triggered."
    },
    {
      "state": "Shown",
      "treatment": "Dark surface, light text, near the trigger."
    }
  ],
  "specs": [
    {
      "property": "Background",
      "value": "Dark grey (~92% opacity), white text (passes AA)"
    },
    {
      "property": "Text",
      "value": "Roboto ~10–12px"
    },
    {
      "property": "Padding / radius",
      "value": "~6px / 4px"
    },
    {
      "property": "Enter delay",
      "value": "~150–500ms"
    }
  ],
  "tokens": [
    "semantic.radius.default",
    "semantic.color.surface.inverse",
    "semantic.color.surface.onInverse",
    "semantic.spacing.xs",
    "semantic.spacing.sm"
  ],
  "props": [
    {
      "name": "variant",
      "type": "'plain' | 'truncation'",
      "required": false,
      "description": "Plain = a short label naming/clarifying the trigger. Truncation reveal = shows the full value when text is clipped."
    },
    {
      "name": "label",
      "type": "string",
      "required": true,
      "description": "Concise text (ideally a few words)."
    },
    {
      "name": "children",
      "type": "React.ReactElement",
      "required": true,
      "description": "The trigger element being described (icon button, truncated text); cloned to receive aria-describedby."
    }
  ],
  "doThis": [
    "Label every icon-only button with a tooltip.",
    "Keep text to a few words.",
    "Trigger on focus too, not just hover."
  ],
  "dontDoThis": [
    "Don't put actions or essential info only in a tooltip.",
    "Don't write long paragraphs.",
    "Don't rely on hover alone (touch/keyboard need an alternative)."
  ],
  "accessibility": [
    "Associate the tooltip with its trigger (e.g. aria-describedby).",
    "Show on focus as well as hover, and allow Escape to dismiss.",
    "The icon button still needs its own accessible name."
  ],
  "related": [
    {
      "name": "Snackbar",
      "why": "Feedback after an action, not on hover"
    },
    {
      "name": "Badge",
      "why": "Persistent status/counts on an icon"
    }
  ]
}
